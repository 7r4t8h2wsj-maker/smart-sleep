import streamlit as st

st.set_page_config(page_title="Smart Sleep", page_icon="🌙")

st.title("🚀 SMART SLEEP v1.0")
st.subheader("Интерактивный гайд для школы №120")

st.markdown("---")

name = st.text_input("Введите ваше имя:")

if name:
    st.write(f"Привет, {name}! Начинаем экспресс-анализ нейрогигиены...")

    bedtime_phone = st.radio(
        "1. Ты использовал телефон в постели сегодня?",
        ("да", "нет")
    )

    sleep_hours = st.number_input(
        "2. Сколько часов ты спал?",
        min_value=0.0,
        max_value=24.0,
        step=0.5
    )

    if st.button("Получить результат"):
        st.markdown("## --- РЕЗУЛЬТАТЫ АНАЛИЗА ---")

        if bedtime_phone == "да":
            st.error("❌ КРИТИЧЕСКАЯ ОШИБКА: Твой мелатонин заблокирован синим светом.")
            st.info("💡 СОВЕТ: Включи Night Shift прямо сейчас!")
        else:
            st.success("✅ МОЛОДЕЦ: Нейронные сети очищены глимфатической системой.")

        if sleep_hours < 8.5:
            st.warning(f"⚠️ ДЕФИЦИТ: Тебе не хватило {round(8.5 - sleep_hours, 1)} ч. до нормы.")

        st.markdown("## 🌙 ТВОЯ МАТРИЦА ЦИКЛОВ")
        st.write("Чтобы проснуться бодрым, используй правило 90 минут:")
        st.write("1. 06:30 (6 полных циклов)")
        st.write("2. 08:00 (7 полных циклов)")

        st.markdown("---")
        st.write("QR-код на полный гайд находится у автора проекта.")
        st.write("Спи крепко, учись легко!")
